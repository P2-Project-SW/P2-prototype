{
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "build:browser": "esbuild DDA/k_means/Elbow_method/elbow_method.ts --bundle --outfile=dist/bundle.js --sourcemap --format=esm --platform=browser",
    "dev": "npm run build:browser"
  },
  "dependencies": {
    "plotly.js-dist": "^3.5.0",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/plotly.js": "^3.0.10",
    "esbuild": "^0.28.0",
    "typescript": "^5.0.0"
  }
}
