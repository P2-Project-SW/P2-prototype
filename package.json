{
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "build": "tsc",
    "watch": "tsc --watch",
    "build:browser": "esbuild DDA/k_means/k_means.ts --bundle --outfile=dist/bundle.js --sourcemap --format=esm --platform=browser",
    "dev": "npm run build:browser || python3 -m http.server || python -m http.server "
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/P2-Project-SW/P2-prototype.git"
  },
  "dependencies": {
    "plotly.js-dist": "^3.5.0",
    "plotly.js-dist-min": "^3.5.1",
    "rxjs": "^7.8.2",
    "tough-cookie": "^6.0.1",
    "tsc": "^2.0.4"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/plotly.js": "^3.0.10",
    "esbuild": "^0.28.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.0.0",
    "vitest": "^4.1.7"
  }
}
